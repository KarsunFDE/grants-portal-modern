# Domain Mapping — grants-portal-modern (derived from acquire-gov)

> Generated by `pair-brownfield-generator` on 2026-05-24. This pair's federal-acquisitions
> aspect is `grants-management` (per D-045 + Cohort #1 Wed-AM pair claim vote — see
> `docs/adrs/0001-grants-management-commitment.md`).

## Aspect summary

| Field | Value |
|-------|-------|
| Primary entity | `GrantApplication` (TitleCase) / `grant_application` (lowercase) / `grant-application` (URL/kebab) |
| Review entity | `PeerReview` / `peer_review` / `peer-review` |
| Java root package | `com.karsunfde.grantsportal` |
| Java segment (entity) | `grantapplication` (no underscore — Java identifier safe) |
| Java segment (review) | `peerreview` |
| Workflow stages | intake → screening → peer-review → award-decision → post-award-reporting |
| Corpus | 2 CFR 200 (Uniform Grants Guidance) + 45 CFR 75 (HHS grants supplement) |
| Corpus dir | `data/seed/grants-uniform-guidance/` |
| Vector index | `grants_uniform_guidance` (collection `COLLECTION_GRANTS_UNIFORM_GUIDANCE`) |
| Agent shape | multi-agent (peer-review-panel orchestrator) |
| Extra service | `panel-coordination-service` (not scaffolded — pair adds W2/W3) |
| Key stakeholders | Grants Officer, Program Officer, Peer Review Panel, Grantee PI, OIG |
| FedRAMP target | MOD (P&E + OIG audit posture) |

## Rename trail (acquire-gov → this repo)

Top-impact renames applied per `skills/pair-brownfield-generator/references/rename-rules.yml`:

| acquire-gov | this repo | scope |
|-------------|----------|-------|
| `services/solicitation-service/` | `services/grant-application-service/` | service-dir |
| `services/evaluation-service/` | `services/peer-review-service/` | service-dir |
| `com.karsunfde.acquiregov` | `com.karsunfde.grantsportal` | Java root package |
| `com.karsunfde.acquiregov.solicitation` | `com.karsunfde.grantsportal.grantapplication` | Java package |
| `com.karsunfde.acquiregov.evaluation` | `com.karsunfde.grantsportal.peerreview` | Java package |
| `class Solicitation` | `class GrantApplication` | TitleCase entity |
| `class Evaluation` | `class PeerReview` | TitleCase entity |
| `solicitation` (lowercase var/column) | `grant_application` | variable/column |
| `evaluation` (lowercase var/column) | `peer_review` | variable/column |
| `Solicitations` / `solicitations` | `GrantApplications` / `grant_applications` | TitleCase + lowercase plurals |
| `Evaluations` / `evaluations` | `PeerReviews` / `peer_reviews` | TitleCase + lowercase plurals |
| `/draft-solicitation` | `/draft-grant-application` | URL path (hyphen form) |
| `/evaluate-solicitation` | `/score-grant-application` | URL path (review_verb=score) |
| `/api/solicitations` | `/api/grant-applications` | URL path |
| `/api/evaluations` | `/api/peer-reviews` | URL path |
| `acquire-gov` (repo + image name) | `grants-portal-modern` | docker-compose, env, CI |
| `acquire_gov` (DB name) | `grants_portal_modern` | Mongo database name |

## DO-NOT-rename allowlist (verified intact)

Per `rename-rules.yml` Phase 3:

- `services/api-gateway/` — cross-aspect architectural DNA; same gateway shape across pairs
- `services/ai-orchestrator/` — Python AI orchestrator stays named (Bedrock InvokeModel wiring identical)
- `frontend/` (Angular SPA directory name)
- `infra/` (Docker Compose + GHA — identical across pairs)
- 12 baseline brownfield-debt markers (see below — all INTACT)

## Brownfield-debt inventory: BASELINE (12) + PAIR-UNIQUE (5)

### Baseline items (12/12 verified intact) — shared with all 3 pairs + acquire-gov

| # | Item | acquire-gov location | this repo location | status |
|---|------|----------------------|--------------------|--------|
| 1 | JWT signature-skip filter | `services/api-gateway/.../JwtSignatureSkipFilter.java` + `SecurityConfig.java` | same (api-gateway not renamed) | INTACT |
| 2 | Audit-log race (recordAsync) | `services/solicitation-service/.../service/SolicitationService.java` + `audit/AuditLogger.java` | `services/grant-application-service/.../service/GrantApplicationService.java` + `audit/AuditLogger.java` | INTACT |
| 3 | No circuit breaker on cross-service AI call | `services/evaluation-service/.../client/SolicitationClient.java` | `services/peer-review-service/.../client/GrantApplicationClient.java` | INTACT |
| 4 | No Pydantic response_model on `/draft-...` | `services/ai-orchestrator/app/main.py` | same — `/draft-grant-application` (4 endpoints) | INTACT |
| 5 | Legacy LangChain `LLMChain.run()` | `services/ai-orchestrator/app/legacy_chain.py` | same | INTACT |
| 6 | Inconsistent correlation-IDs across services | api-gateway logs `X-Request-ID`, solicitation logs `correlationId`, evaluation logs `traceId`, ai-orchestrator logs nothing | grant-application logs `correlationId`, peer-review logs `traceId`, api-gateway logs `X-Request-ID`, ai-orchestrator logs nothing | INTACT |
| 7 | `pinecone-client` unused dep | `services/ai-orchestrator/requirements.txt` | same | INTACT |
| 8 | Hardcoded localhost URL in Angular component | `frontend/src/app/components/solicitation-list/solicitation-list.component.ts` | `frontend/src/app/components/grant-application-list/grant-application-list.component.ts` (URL also rewritten to `/api/grant-applications`) | INTACT |
| 9 | No Jsoup.clean on description | `services/solicitation-service/.../service/SolicitationService.java` + `dto/SolicitationCreateRequest.java` | `services/grant-application-service/.../service/GrantApplicationService.java` + `dto/GrantApplicationCreateRequest.java` | INTACT |
| 10 | No multi-tenant `agencyId` filter (listAll → findAll) | `services/solicitation-service/.../service/SolicitationService.java` + `repository/SolicitationRepository.java` | `services/grant-application-service/.../service/GrantApplicationService.java` + `repository/GrantApplicationRepository.java` | INTACT |
| 11 | Dockerfile `:latest` tags (4 of 5 Dockerfiles) | `services/*/Dockerfile` + `frontend/Dockerfile` (ai-orchestrator pinned to 3.11-slim — that's the exception, see Item 11 in `docs/brownfield-debt.md`) | same | INTACT |
| 12 | GHA workflow lint step disabled | `.github/workflows/ci.yml` + `infra/github-actions/ci.yml` | same | INTACT |

### Pair-unique items (5/5 injected per D-059) — distinct across pairs

| id | category | inject mode | location | difficulty | scheduled unlock |
|----|----------|-------------|----------|------------|------------------|
| `sec-cors-wildcard-credentials` | security | edit_existing | `services/grant-application-service/src/main/java/com/karsunfde/grantsportal/grantapplication/SecurityConfig.java` | medium | W4 |
| `rel-db-pool-size-one` | reliability | edit_existing | `services/grant-application-service/src/main/resources/application.yml` | medium | W5 |
| `obs-pii-in-info-logs` | observability | edit_existing | `services/grant-application-service/src/main/java/com/karsunfde/grantsportal/grantapplication/service/GrantApplicationService.java` (+ `dto/GrantApplicationCreateRequest.java`) | easy | W5 |
| `ai-bedrock-streaming-unhandled` | ai-integration | edit_existing | `services/ai-orchestrator/app/bedrock_client.py` | medium | W3 |
| `rel-file-upload-local-filesystem` | reliability | create_new | `services/grant-application-service/src/main/java/com/karsunfde/grantsportal/grantapplication/service/AttachmentService.java` | medium | W5 |

Distribution constraints (verified):
- 5 items in pair set (target range 4-6 — OK)
- All 5 are unique to Pair 1; do not appear in Pair 2 (`cohort_1_pair_2_contract`) or Pair 3 (`cohort_1_pair_3_foia`) recipes
- Spans 4 of 4 categories (security, reliability, observability, ai-integration) — exceeds ≥3 minimum
- 0 hard-difficulty items — well under the ≤1 cap

Locked-failing tests for each item live alongside the existing baseline debt tests:
- `services/grant-application-service/src/test/java/com/karsunfde/grantsportal/grantapplication/CorsWildcardCredentialsDebtTest.java`
- `services/grant-application-service/src/test/java/com/karsunfde/grantsportal/grantapplication/DbPoolSizeOneDebtTest.java`
- `services/grant-application-service/src/test/java/com/karsunfde/grantsportal/grantapplication/PiiInInfoLogsDebtTest.java`
- `services/grant-application-service/src/test/java/com/karsunfde/grantsportal/grantapplication/FileUploadLocalFilesystemDebtTest.java`
- `services/ai-orchestrator/tests/test_bedrock_streaming_unhandled_debt.py`

All carry `@Tag("brownfield_debt")` (Java) / `@pytest.mark.brownfield_debt` (Python). Excluded from default `mvn test` / `pytest` runs per existing acquire-gov surefire `excludedGroups` + pytest marker convention.

## bug_sketch adaptations (per SKILL Step 4b)

- **`obs-pii-in-info-logs`** — pool bug_sketch references `applicant.getSsn()` + `applicant.getFullName()` on an Applicant entity. Adapted to grants-context fields on `GrantApplicationCreateRequest`: `principalInvestigatorName` + `applicantSsn`. The PI-PII pattern fits grants-management's stakeholder set better than a generic applicant entity (PIs submit applications in NIH/NSF grant flows).
- **`sec-cors-wildcard-credentials`** — pool bug_sketch presents a clean `@Bean CorsConfigurationSource` in isolation. Adapted to coexist with the existing era-authentic Spring Security 5 `WebSecurityConfigurerAdapter` pattern (`http.cors().and()...`). The wildcard + credentials combo is preserved verbatim.
- **`rel-file-upload-local-filesystem`** — pool `locked_failing_test` proposes pod-restart simulation. Adapted to assert the S3-URI return-shape contract instead (lighter test; same intent). Noted in test docstring.
- **`rel-db-pool-size-one`** — pool `locked_failing_test` runs 10 parallel HTTP calls against a live Spring app. Adapted to inspect `application.yml` config-bytes via SnakeYAML (lighter; same intent). Noted in test docstring.

## Additional inherited entities (post-D-060)

Per `pair-brownfield-generator/SKILL.md` §Step 3 note, the renamed services carry 15
additional entities beyond `GrantApplication` + `PeerReview` (Vendor, Proposal, Amendment,
Qna, Award, Contract, ContractModification, Cpar, Finding, QaspFinding, AuditEvent,
ClauseLibraryEntry, User, Deliverable, DebriefRequest). These are **raw material the
pair may repurpose, rename, or delete** in their modernization PRs (W4–W5). The pair's
authority to reshape them is explicit:

- Repurpose (likely): `Vendor` → Awardee organization, `Proposal` → Application, `Award` → Grant Award, `User` → Grantee PI / Reviewer / Program Officer
- Delete (likely): `Cpar`, `ContractModification`, `Contract` (post-award contract concepts that don't map to grants — though `QaspFinding` could double for grant-monitoring findings)
- Rename in modernization: pair decides

This is intentional richness, not a leak. The 12 baseline debt items live in the renamed
core services; the 15 additional entities are domain-edge.
