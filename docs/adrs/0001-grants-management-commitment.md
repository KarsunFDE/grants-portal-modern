# ADR-0001 — Grants-Management Aspect Commitment (Cohort #1 Pair 1)

Status: ACCEPTED
Date: 2026-05-24
Decider: pair-brownfield-generator (Cohort #1 Pair 1)
Source: D-045 + D-059 + Cohort #1 Wed-AM claim vote (placeholder — pair confirms on join)

## Context

Per `fde-10-week/pipeline/DECISIONS.md` D-045, each pair's Pair Project is anchored to a
distinct slice of Karsun's federal-acquisitions / modernization universe. The aspect
catalog lives in `fde-10-week/skills/scenario-design-planning/references/karsun-domain-aspects.yml`.

Per D-059, the per-pair brownfield is generated from `acquire-gov` via the
`pair-brownfield-generator` skill. This ADR is the canonical record of the aspect
choice for this pair.

## Decision

**This pair commits `grants-management` (Federal Grants Management) as their Pair
Project aspect.**

- Primary entity: `GrantApplication`
- Review entity: `PeerReview`
- Workflow stages: intake → screening → peer-review → award-decision → post-award-reporting
- Corpus type: grants regs (2 CFR 200 Uniform Grants Guidance + 45 CFR 75 HHS supplement)
- Key stakeholders: Grants Officer, Program Officer, Peer Review Panel, Grantee PI, OIG
- Agent shape: multi-agent (per `karsun-domain-aspects.yml` ai_adoption_phase_1_seeds)

## Phase-1 AI capabilities (initial commitment)

Drawn from `karsun-domain-aspects.yml` `ai_adoption_phase_1_seeds`:

1. LLM-assisted application screening (Section 508 + responsive-language flags)
2. RAG over prior award abstracts to surface duplicate-funding risk
3. Multi-agent review-panel orchestrator (peer-reviewer assignment + conflict detection)

Pair revises this commitment on W2 Mon Plan Day as RAG specifics get clearer.

## Phase-2 modernization arc (initial guess)

1. AI-SRE for application-deadline traffic spikes (auto-scale + queue mgmt)
2. OWASP LLM Top 10 hardening on PI-submitted free-text fields (prompt injection through
   grant narratives)
3. Multi-tenant boundary tests proving Agency-A grantees can't read Agency-B awards

## Distinctness from training-project (acquire-gov)

acquire-gov ships solicitation-drafting + evaluation orchestration. grants-management
inverts the workflow toward application-screening + panel-coordination. Different
stakeholders, different NFRs (10× traffic on submission deadlines vs steady-state),
different corpus (2 CFR 200 not FAR Part 15).

## Generator output

This repo was generated by `pair-brownfield-generator` on 2026-05-24, source brownfield
`acquire-gov` commit `10bc30e`. The reshape preserves all 12 baseline brownfield-debt
items + injects 5 pair-unique items per `pair-unique-debt-pool.yml` recipe
`cohort_1_pair_1_grants` + 4 stretch-backlog items per `stretch-backlog-pool.yml` recipe
`cohort_1_pair_1_grants`.

See `domain-mapping.md` for the rename trail + debt inventory.
See `docs/pair-unique-debt.md` for the cohort-visible pair-unique debt list.
See `BACKLOG.md` for the stretch backlog.
