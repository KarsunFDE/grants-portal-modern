Workflow:
1. Intake
2. Screening
3. Peer Review
4. Award

HITL Gates:

We use human approval at key risk points.

Gate 1: Eligibility and Risk Review
	Stage: Screening
	Owner: Grants Officer or Program Officer
	Input: Application data, eligibility checks, risk flags, AI recommendation
	Allowed decisions:
		- Approve to move forward
		- Return for fixes
		- Reject
 - Rule: No application can move past Screening without a human decision and rationale.
 
Gate 2: Conflict of Interest (COI)
	Stage: Peer Review
	Owner: Review Lead
	Input: Reviewer assignments, COI flags, reviewer disclosures
	Allowed decisions:
		- Resolve and continue
		- Remove reviewer and reassign
		- Override with written rationale
 - Rule: No scoring starts until COI is resolved by a human.
 
Gate 3: Factor Suggest Acceptance
	Stage: Peer Review
	Owner: Human reviewer
	Input: AI factor suggestion output
	Allowed decisions:
		- Accept
		- Edit
		- Reject
 - Rule: AI suggestions are assistive only and require human acceptance before use in scoring narrative.
 
Gate 4: Award Decision
	Stage: Award
	Owner: Grants Officer
	Input: Peer review results, recommendation package, AI-assisted summaries
	Allowed decisions:
		- Award
		- Do not award
		- Return to review
- Rule: No unattended award path exists. Award must have a persisted human approval record and rationale.

Grounding Policy:
For regulatory or policy answers:

- AI must reference real sources: 2 CFR 200, 45 CFR 75, and NOFO content.
- If output is low-confidence, missing citations, or ungrounded:
	- Block output
	- Escalate to human review
- Rule: Ungrounded regulatory guidance is never directly shipped.

Audit and Durability Requirements:
Every gate decision must be logged in an append-only trail:

 - actor_id
 - actor_role
 - tenant_id
 - timestamp
 - workflow_stage
 - gate_id
 - ai_run_id
 - decision
 - rationale
 - override_flag
 - evidence_refs

System requirements:

 - Logs survive restart
 - Logs support multi-day pause and resume
 - OIG can reconstruct decisions from logs alone
 - No cross-tenant data leakage
 
Correct Regulatory Mapping
 - 2 CFR 200.205 = merit review
 - 2 CFR 200.206 = risk review
 
Acceptance Criteria
1. Award cannot be completed without human approval record.
2. Screening cannot advance without Gate 1 decision.
3. Peer review scoring is blocked until COI gate is resolved.
4. Factor Suggest output cannot be used until human acceptance.
5. Ungrounded regulatory output is blocked and escalated.
6. Audit trail captures all required gate fields.
7. Audit trail is durable across restart and pause.
8. Tenant boundaries are enforced in logs and gate evidence



Retrieval Planning:

Goal:
Design a retrieval strategy for out grants application that:
	- Grounds AI outputs in domain sources
	- Use ONLY AWS Bedrock models for complaiance reasons
	- Minimizie token usage and model spend
	- Escalates low-confidence cases to HITL instead of consistent retries
	
Architecture:
We need langchain v1.0 inside of our system, this moves away from LCEL, Runnables & Chains. Please ground all truth behind langchain v1.0 through documentation ONLY. Never use public facing resources from the wider community.
We currently use a MongoDB with seed data, we need to address this by also include MongoDB Atlas for Vector DB capabilities. We need to roll our MongoDB container into a local MongoDB Atlas container to support both databse usage & vector db.
We need to consider what size & embedding model we leverage for our MongoDB atlas container. WE need to make sure, these models exist & can be leveraged through Bedrock without and major additional steps or deployments. Should be usable just with an API Key. Consider lower cost as an Ideal solution.

Guidelines:
Keep additional costs to a minimum, we are working on a hairpin budget.
We must use models within the AWS Bedrock workspace, wing new, it should be queriable through AWS Bearer Tokens.
Make sure to have a rollover plan for our MongoDB > MongoDB Atlas for storage of our database & documents for retrieval. It supports both systems. We don't want to lose information during this migration step.
We must confirm everything through our plan & address Astions with a back & forth question-answer style.
We should focus on using established software, packages & tooling, don't opt to homegrow our own implementation just because we can. Require approval for anything where we NEED custom implementation.
Perform some additional research to ensure we've covered our basis for any considerations & questions we continue to have. We may have missed some key information for our system.

Assumptions and Constraints
Bedrock-only model policy is mandatory.
Budget is constrained, cost optimization is a hard requirement.
Retrieval is required before generation for policy-sensitive outputs.
Official LangChain v1 documentation is the only source of framework guidance.
Existing MongoDB-backed app data must remain intact through migration to Atlas-backed retrieval.

In Scope (Phase 1)
Corpus inventory and ingestion plan.
Hybrid retrieval design (keyword + semantic).
Tenant-aware filtering and access control.
Confidence + citation checks with HITL escalation.
Initial observability, evaluation, and cost controls.
MongoDB-to-Atlas retrieval migration plan with rollback.

Out of Scope (Phase 1)
Full autonomous decisioning.
Advanced multi-agent orchestration redesign.
Full post-award retrieval expansion.
Custom in-house retrieval framework implementation.

Corpus Plan
Primary sources:
 - 2 CFR Part 200.
 - 45 CFR Part 75.
 - NOFO and amendments.
 - Agency policy and approved guidance.
 - Approved historical Q&A (tenant-scoped).

Considerations/Questions:
How are we to secure this applications RAG documents? Sal govt information.
What type of retrieval patterns will best support our particular use case(s)? We have a few different parts that may need differen types of retrievals. Such as, dense, sparse or hybrid.,
What are some of the best re-ranking strategies for retrieval?,
How do we handle RAG observability for requiests?,
What anti-patterns exists that we should consider.,
What's RAGAS? How do we collect metrics for evalaution,
What's LLM-as-a-judge for retrieval?,
How do we appropriately manage the index?,
What do we do during any failure points with retrieval?,
How do we manage multi-tenant retrieval processes?,
We need to make sure we ground all of our retrieval inggging. This way they now how things were constructed andwho done it.,
How do we chunk all of our documentation? What should,
Is there any consideration to Caching retrieval at startup?,
HITL stages for RAG.,
What tools exists from langchain v1.0 that support our systems so we don't have to re-invent the wheel.
